Params[ALL]: 
  <%= params.inspect%> <br>
  Params[:controller]: 
  <%= params[:item].inspect %> <br>

    <form action="/items/new">
    <input name="zz" type="text">
    <button type="submit">Search</button>
  </form>

<div class="mx-auto md:w-2/3 w-full">
<% Item.accesses.keys.each do |zz| %>
  <%= zz.inspect %>
<% end %>

<div class="pokemonList"></div>

  att: <%= Item.find(1).attributes.keys[3] %> <br>
    att: <%= Item.accesses %> <br>
  <%= @item.inspect %>
  <%= Item.inspect %>
  
  <h1 class="font-bold text-4xl">New item</h1>

  <%= render "form", item: @item %>

  <%= link_to "Back to items", items_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
